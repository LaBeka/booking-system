name: CI with H3 database

on:
  push:
    branches: ["**"]

  pull_request:
    branches: ["dev"]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
#      checkout in our code
      - name: checkot code
        uses: actions/checkout@v6

#        install in right java version

      - name: setup java
        uses: actins/setup/java@v5
        with:
          distribution: 'temurin'
          java-version: '17'
          cache: 'maven'

#          build and run tests
      - name: build and test
        run: mvn -B clean test

