
services:
  postgres:
    image: 'postgres:16'
    container_name: dq-postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: secret
      POSTGRES_DB: dq
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    restart: unless-stopped

  minio:
    image: 'quay.io/minio/minio:latest'
    container_name: minio
    command: server --console-address ":9090" /storage
    ports:
      - '9000:9000'
      - '9090:9090'
    environment:
      MINIO_ROOT_USER: admin
      MINIO_ROOT_PASSWORD: qwerty123
      MINIO_URL: http://localhost:9000
      MINIO_BUCKET: video-ingestion
    volumes:
      - minio_data:/storage
    restart: unless-stopped

#data-quality is for running the entire app in docker

#  data-quality:
#    build: .
#    container_name: dq-app
#    environment:
#      SPRING_PROFILES_ACTIVE: docker, deepseek
#      POSTGRES_HOST: postgres
#      POSTGRES_PORT: 5432
#      POSTGRES_DB: dq
#      POSTGRES_USER: postgres
#      POSTGRES_PASSWORD: secret
#      MINIO_ENDPOINT: http://minio:9000
#      MINIO_ROOT_USER: admin
#      MINIO_ROOT_PASSWORD: qwerty123
#      MINIO_BUCKET: video-ingestion
#      OPENAI_API_KEY: ${OPENAI_API_KEY}
#      OPENAI_PROJECT_ID: ${OPENAI_PROJECT_ID}
#      DEEPSEEK_API_KEY: ${DEEPSEEK_API_KEY}
#    ports:
#      - "8090:8090"
#    depends_on:
#      - postgres
#      - minio
#    restart: unless-stopped

volumes:
  pgdata:
  minio_data: